jQuery(document).ready(function(){
    $("nav a").mPageScroll2id({
        offset:60,
        scrollSpeed: 1200
        
    });
    $("a").mPageScroll2id({
        offset:60,
        scrollSpeed: 1200
    });

});